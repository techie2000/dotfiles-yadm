# ok if certification validation fails
# only do this IF you are sure you undestand the possible consequences
#note, this is the long form of -k option
--insecure

# follow redirects
--location

#trace the time taken. more info here https://everything.curl.dev/usingcurl/verbose/trace
--trace-time

